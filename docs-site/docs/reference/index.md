# Reference

The contracts, listed. Every name here exists in `arche-core` 0.9.0; a signature is copied from the source, not paraphrased.

- [Python API](python-api.md): every public verb and type, by job.
- [Command line](cli.md): every `arche` subcommand with its options.
- [HTTP API](http-api.md): every route, its body and its response.
- [MCP tools](mcp-tools.md): every tool, its arguments, and what it never returns.
- [Extras](extras.md): what each `pip install "arche-core[...]"` adds, and why it is optional.
- [Benchmarks](benchmarks.md): every number the site claims, including the runs where arche loses.
